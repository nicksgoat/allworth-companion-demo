# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v56.0.0/ before writing any code.

# Design system

Before any UI work, read `DESIGN.md` (this directory) — tokens, type ladder,
component table, motion and haptics rules. Values not drawn from `src/theme.ts`
tokens are design bugs.

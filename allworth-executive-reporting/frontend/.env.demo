# Local no-auth preview mode (loaded by `npm run dev:demo`, i.e. `vite --mode demo`).
# Renders bundled demo metrics with NO authentication and NO backend/Synapse.
# VITE_ENTRA_CLIENT_ID is intentionally left unset so SSO stays disabled.
VITE_DEMO_MODE=true
